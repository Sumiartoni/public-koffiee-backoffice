import React from 'react';

export default function CustomerVouchersManager() {
    return (
        <div className="p-10 text-white">
            <h1 className="text-3xl font-bold">Voucher Manager (DEBUG MODE)</h1>
            <p>If you see this, the component integration is working.</p>
        </div>
    );
}
